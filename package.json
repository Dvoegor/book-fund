{
  "name": "book-fund",
  "version": "1.0.0",
  "engines": {
    "node": "12.x"
  },
  "scripts": {
    "start": "npm install --prefix client && npm run build --prefix client && npm install --prefix server && node server/server.js",
    "server": "nodemon server/server.js",
    "client": "cd client && npm run start",
    "dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\"",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix client && npm run build --prefix client && npm install --prefix server && node server/server.js"
  },
  "author": "Grigorii Dvoeglazov",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.2.1"
  }
}
